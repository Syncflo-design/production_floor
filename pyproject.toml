[project]
name = "production_floor"
authors = [
    { name = "NestERP / Manifold SA" }
]
description = "Simplified floor operator screens for ERPNext manufacturing"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "production_floor"
